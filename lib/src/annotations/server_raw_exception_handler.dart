part of annotations;

//################################################################
/// Annotation for a raw server exception handler function.

class ServerRawExceptionHandler extends WoomeraAnnotation {
  /// Constructor for low-level server exception handler annotation.
  ///
  /// A program can have at most one such annotation.

  const ServerRawExceptionHandler();
}
