part of annotations;

//################################################################
/// Abstract base class for all Woomera annotation classes.

abstract class WoomeraAnnotation {
  /// Constructor
  const WoomeraAnnotation(); // must be const to work as an annotation
}
